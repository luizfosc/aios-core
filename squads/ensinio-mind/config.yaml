name: ensinio-mind
version: 3.0.0
description: "Ensinio Knowledge Hub — Source of truth centralizada sobre a plataforma Ensinio. Alimenta todos os squads ensinio-*. Inclui sales playbook (8 objeções + 7 técnicas campo), ICP (5 novos nichos), red flags (26 sinais), 6 novos concorrentes, ciclo de venda (4 padrões), pricing AI Agents, padrões de migração reais."
type: knowledge-hub

entry_agent: ensinio-expert

agents:
  - ensinio-expert

tasks:
  - update-kb.md              # Processo de atualização do KB (fontes estruturadas)
  - validate-data.md          # Validação de consistência entre arquivos
  - extract-from-calls.md     # Extração de insights de calls tl;dv (fontes não-estruturadas)

checklists:
  - data-quality.md        # Qualidade dos dados
  - seller-onboarding.md   # Onboarding de novo vendedor

data:
  # Identidade & Posicionamento
  - ensinio-identity.md
  # Ecossistema de Produtos (Club, Pay, Stream, AI, Engage, Chat)
  - ensinio-product-ecosystem.md
  # KB Completo — 5 pilares, 67+ features com keywords
  - ensinio-solutions-kb.md
  # Pricing, planos, taxas, add-ons
  - ensinio-pricing.md
  # Matriz competitiva detalhada
  - ensinio-competitors.md
  # Cases, depoimentos, prova social, links demonstráveis
  - ensinio-cases.md
  # Mercado: TAM/SAM/SOM, receita histórica, dados comerciais
  - ensinio-market.md
  # Critérios de qualificação de leads (dual scoring)
  - scoring-criteria.md
  # Regras de comunicação + scripts do time comercial
  - message-rules.md
  # ICP detalhado — Perfis demográfico + comportamental
  - ensinio-icps.md
  # Red flags — 21 sinais de alerta e perfis a evitar
  - ensinio-red-flags.md
  # Sales playbook — 8 objeções + técnicas avançadas de fechamento
  - ensinio-sales-playbook.md
  # Sales cycle — Timeline, funil, equipe, onboarding
  - ensinio-sales-cycle.md
  # Argumentos de venda — 3 principais + como usar em cada fase
  - ensinio-arguments.md
  # Padrões de onboarding/suporte — fricções, FAQs, setup por nicho
  - ensinio-onboarding-patterns.md

settings:
  language: pt-BR
  model_tier: sonnet
  activation_protocol: base

metadata:
  created: "2026-03-02"
  updated: "2026-03-14"
  created_by: squad-chief
  sources:
    - "ensinio.com (site oficial)"
    - "Pitch Deck Ensinio 2026 (52 slides)"
    - "whatsapp-prospector/data/ (KB v2.0.0)"
    - "Google Forms — Gui Ávila (06/03/2026)"
    - "Google Forms — Antonio (12/03/2026)"
    - "Google Forms — Ricardo (13/03/2026)"
    - "tl;dv calls — Ricardo (vendas) + Bhrenda (suporte)"
    - "tl;dv batch 1 (5 calls processadas 2026-03-14): 69b45eb, Como Elevar, Como implementar agentes, Como automatizar vendas, Continuação Luiz e Flávia"
  consumers:
    - whatsapp-prospector
    - "(futuros squads ensinio-*)"
  pending_data:
    - "ensinio-partnership-program.md — Programa de parceiros/revendedores (v3.0)"
    - "ensinio-content-strategy.md — Estratégia de conteúdo (v3.0)"
