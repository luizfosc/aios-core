# ETL Universal Converter — Python Dependencies
# Install: pip3 install -r requirements.txt

# Core data processing
polars>=0.20.0           # Fast DataFrame library for CSV/Excel
openpyxl>=3.1.0          # Excel .xlsx parser with multi-sheet support
tabulate>=0.9.0          # Convert tabular data to markdown tables

# Presentation processing
python-pptx>=0.6.21      # PowerPoint .pptx text and notes extraction

# OCR (image processing)
pytesseract>=0.3.10      # Tesseract OCR wrapper
Pillow>=10.0.0           # Image processing for OCR prep

# Text processing
chardet>=5.0.0           # Encoding detection
langdetect>=1.0.9        # Language detection

# Validation
pydantic>=2.0.0          # Schema validation for frontmatter
PyYAML>=6.0              # YAML frontmatter generation

# Optional (for enhanced features)
# pandas>=2.0.0          # Fallback for polars edge cases
# lxml>=4.9.0            # Enhanced XML parsing
